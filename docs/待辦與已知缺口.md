# 待辦與已知缺口

累積的未完成項目與已知風險，取代「只活在對話記憶裡」的狀態。完成後把整條移到
文件最下方的「已解決」並標日期，不要直接刪除（保留軌跡）。

新增項目時盡量標**來源決策**（連結 `decisions/000N-*.md`），方便回溯為什麼會有這個缺口。

## 未解決

| 項目 | 風險 | 來源 |
|------|------|------|
| `canAccessFactory`/`requireFactoryAccess`（reporter 只能存取自己綁定廠）尚未套用到多數 route | 目前 4 帳號皆 admin 無曝險；未來若建 reporter 帳號，廠別隔離不保證 | [0004](decisions/0004-admin-api-authorization.md) |
| `admin_audit_log` 沒有前端頁面可看，只能直接查 DB | 查證單位若要看軌跡，目前得請 Claude 幫忙查 SQL | [0005](decisions/0005-admin-audit-log.md) |
| `pg_dump` / `psql.exe` 被部署機器 OS 擋（Access denied），資料庫備份做不了 | 無法定期備份，出事只能靠雲端 DB 的 point-in-time restore | [0001](decisions/0001-factory-lifecycle.md) |
| 192.168.6.100 內網 port 未開通，內網 Git（Gitea/GitLab 第二 remote）與 DB 搬遷都卡著 | 目前唯一真正的版控備援是 GitHub | [0001](decisions/0001-factory-lifecycle.md) |
| 身分層 + 查證封存（V40/V41）已套用 DB，尚未部署到 pm2 | 部署前必須先建帳號，順序錯了登入頁會沒有帳號可用 | [0003](decisions/0003-identity-verification-freeze.md) |
| 登入是 email+密碼過渡方案，尚未接 Azure AD SSO | 密碼管理責任在使用者端，非公司 SSO 治理範圍 | [0003](decisions/0003-identity-verification-freeze.md) |
| `數據覆蓋與查證封存_設計文件.md` 曾有 v1.0 與程式碼不符 8 處 | 若沒有機制檢查同步，spec 會再度漂移，誤導下一個接手的人 | [0003](decisions/0003-identity-verification-freeze.md) |

## 已解決

- Admin API（`/api/admin/*`）完全沒有權限檢查 — 2026-08-26 補上 `requireAdmin()`，
  見 [0004](decisions/0004-admin-api-authorization.md)
- 無變更稽核 log — 2026-08-26 新增 `admin_audit_log`（V65），見 [0005](decisions/0005-admin-audit-log.md)
