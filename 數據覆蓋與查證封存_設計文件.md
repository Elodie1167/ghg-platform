# 數據覆蓋與查證封存 — 設計文件

**版本**：v1.2（草案，待確認）
**建立日期**：2026-08-10
**更新日期**：2026-08-11
**撰寫**：永續發展部 Elodie
**狀態**：設計決議完成，migration 尚未產出
**對應開發項目**：GHG 盤查平台（身分建置 V40 + 查證封存 V41）

> ⚠️ 本文件涉及第三方查證與對外揭露機制，**需永續發展部及查證單位確認**，不下最終結論。

---

## 修訂紀錄

### v1.1（2026-08-11）— 比對程式碼與資料庫後修正 5 處

| # | v1.0 說法 | 修正 |
|---|---|---|
| 1 | 產出 `V36__verification_freeze.sql` | **改為 `V41`**。V36 已被佔用，DB 已套用至 V39（共 39 支），V40 給身分建置 |
| 2 | 比對鍵含 `invoice_no`，掛在 `activity_records` | **下移至 line item 層**。`invoice_no` 在 `activity_line_items`，原設計跨兩表無法成立（§3.2） |
| 3 | 匯入模式全域三選一 | **改為分路徑定義**。現行有「固定分頁」與「單據明細」兩條語意不同的路徑（§4.2） |
| 4 | §8 以 `users.can_freeze` 控管 | **新增前置條件 §0**。`users` 表 0 列、登入為硬寫共用帳號，`can_freeze` 無從檢查 |
| 5 | §6.4 靠 DB 層 REVOKE 達成唯讀 | **改為 REVOKE + trigger 雙保險**。app 連線角色可能為表 owner（§6.4） |

新增 §0（身分前置條件）、§6.6（與範疇二整年重算的衝突）、§5.3 修正（區分人為改值與系統重算）。

### v1.2（2026-08-11）— 依決議調整三處核心設計

| # | v1.1 說法 | v1.2 修正 |
|---|---|---|
| 6 | 走 Azure AD SSO，不做過渡方案 | **改為先做密碼登入過渡**。IT 排程不明，封存不能一直卡著（§0.3） |
| 7 | 176 筆已檢核記錄的檢核者無法回溯補齊 | **可以補齊**。經確認該期間平台僅 Elodie 一人操作，以 migration backfill（§0.2） |
| 8 | 靠「疑似重複」判定解決識別碼缺漏，並把該介面做成一級功能 | **改為由使用者宣告匯入模式**，重複問題在源頭消失，不需要複雜的比對介面（§3.4、§4.2） |

---

## 一、問題背景

平台允許使用者以 Excel 匯入活動數據，且業務規則允許「同廠 + 同排放源 + 同年月多筆記錄」（多張電費單、多台公務車各一筆）。因此當使用者重複上傳時，系統無法自動判斷新資料是要：

- **覆蓋**既有資料（更正）
- **補填**尚未填報的月份（續填）
- **新增**另一筆平行記錄（第二台車、第二張單）

同時衍生兩個層級的資料保護需求：

1. 部門內部檢核完成的資料 → 可覆蓋，但須提醒
2. 第三方查證完成的資料 → **絕對不可更動**

本文件定義上述兩者的完整設計。

---

## 二、核心設計原則

1. **由使用者宣告意圖，系統不猜**（v1.2 強化）：匯入時使用者先選「這是整月完整檔」或「這是補單」，系統依宣告執行，不從資料反推意圖。
2. **匯入不直接寫入資料庫**：一律先進暫存區（staging），把「將發生什麼」攤開給使用者確認後才寫。
3. **兩層保護分離**：「檢核」是軟性提醒，「查證封存」是硬性禁止，機制完全不同。
4. **對外揭露讀快照**：報告書與對外數字一律讀封存快照表，不讀主表。

---

## ○、前置條件：使用者身分

### 0.1 現況（2026-08-11 實測）

| 項目 | 實際狀況 |
|---|---|
| `users` 表列數 | **0 列**（從未建立任何帳號） |
| `users` 實際欄位 | `id`, `azure_oid`, `email`, `display_name`, `is_active`, `created_at` |
| 登入機制 | `lib/auth.ts` 為 NextAuth Credentials，**帳密硬寫在程式碼中**，全平台共用一組，回傳的 `id` 不是 `users` 表的 UUID |
| `activity_records` 總筆數 | 241（2025 年 78 筆、2026 年 163 筆） |
| 其中 `created_by IS NOT NULL` | **0 筆** |
| 其中 `is_reviewed = true` | **176 筆**（2025 年 70 筆、2026 年 106 筆） |
| 其中 `reviewed_by IS NOT NULL` | **0 筆** |
| `activity_line_items` 總筆數 | 509（`invoice_no` 有值 211 筆／41%，`erp_ref` 有值 283 筆／56%） |

### 0.2 既有檢核記錄的補齊（v1.2 修正）

**問題**：176 筆記錄標記為「已檢核」，但 `reviewed_by` / `reviewed_at` 全為 NULL。查證單位若追問「這些數據由誰複核」，平台原本無法自證。

**✅ 2026-08-11 確認可補齊**：經 Elodie 確認，**該期間平台僅 Elodie Cheng 一人操作**，不存在其他檢核者。故可於身分建立後以 migration 回填：

```sql
-- 回填理由：經永續發展部確認，本平台自建置起至 2026-08-11 期間
-- 僅 Elodie Cheng（elodiecheng@makalot.com.tw）一人具有存取與檢核權限，
-- 不存在其他檢核者。原 reviewed_by 為 NULL 係因當時登入機制為共用帳號、
-- 未接 users 表，非檢核行為缺失。
UPDATE activity_records
   SET reviewed_by = (SELECT id FROM users WHERE email = 'elodiecheng@makalot.com.tw')
 WHERE is_reviewed = TRUE
   AND reviewed_by IS NULL;
```

`reviewed_at` 無從回溯（未曾寫入），維持 NULL；此限制須於查證時如實說明，不得補假時間。

> ⚠️ 此回填屬**事實補正**而非資料變更：檢核行為確實發生，只是當時系統未記錄執行者。migration 註解須完整保留上述理由，供查證查閱。**不得**在無單一操作者事實依據的情況下沿用此做法。

### 0.3 登入方式決議（v1.2 修正）

**v1.1 原決議**：直接走 Azure AD SSO，不做過渡方案。
**v1.2 改為**：**先做密碼登入過渡，SSO 之後再接。**

**改變理由**：Azure AD 需 IT 配合，**排程完全未知**；而封存功能的權限控管與稽核留痕全部依賴身分，等 SSO 等於讓封存無限期卡住，有趕不上第一家廠查證日的實質風險。

**過渡方案的替換成本評估（低）**：

- `users` 表結構不變，帳號一律以 `email` 為識別鍵
- SSO 上線時只需替換 `lib/auth.ts` 的 `authorize()` 驗證段，並於同一列補上 `azure_oid`
- **既有填報、檢核、封存記錄全部不需搬移**（外鍵指向 `users.id`，UUID 不變）
- 需拆除的僅為登入頁的密碼欄位與該段驗證邏輯

### 0.4 `V40__user_identity.sql`（身分建置）

```sql
ALTER TABLE users
  ADD COLUMN password_hash VARCHAR(100),          -- bcrypt；SSO 上線後此欄位設 NULL 停用
  ADD COLUMN role          VARCHAR(10) NOT NULL DEFAULT 'reporter'
                           CHECK (role IN ('reporter', 'admin')),
  ADD COLUMN factory_id    UUID REFERENCES factories(id),  -- reporter 綁廠；admin 為 NULL
  ADD COLUMN can_freeze    BOOLEAN NOT NULL DEFAULT FALSE; -- 即 §8.2
```

`can_freeze` 於本支一併加入，§6 的封存 migration 不再需要動 `users`。

**密碼處理原則**：
- 以 bcrypt 雜湊儲存，**明文密碼不寫入任何檔案、腳本或 commit**
- 由 `scripts/create-user.mjs` 於執行時由操作者輸入
- 密碼循公司密碼保管流程

### 0.5 初始帳號（✅ 2026-08-11 決議）

| Email | 姓名 | 角色 | `can_freeze` | 說明 |
|---|---|---|---|---|
| elodiecheng@makalot.com.tw | Elodie Cheng | admin | ✅ | 主責 |
| JohnsonLin@makalot.com.tw | Johnson Lin | admin | ✅ | 主管，第二位封存權限人（解除 §8.4 單點失效） |
| kellylin@makalot.com.tw | Kelly Lin | admin | ❌ | 日常使用者 |
| mengyinghong@makalot.com.tw | Meng-Ying Hong | admin | ❌ | 日常使用者 |

**`can_freeze` 僅 Elodie 與 Johnson 持有**。封存為不可逆操作且對應對外揭露，日常填報與檢核不需此權限。

### 0.6 本輪刻意不做的事

- **不補現有 API 的廠別權限過濾**。目前所有 API 皆無 reporter 廠別過濾；一次修改 20 餘個 route 風險過高，需逐頁驗證。本輪只建立身分，過濾另開一輪。
  - 現階段四個帳號皆為 admin，無實際曝險。
- **不做 `/admin/users` 帳號管理頁**（§8.3 第 2 層）。初期靠 CLI 腳本，UI 隨封存功能一併開發。

---

## 三、識別碼（Natural Key）設計

### 3.1 現況確認

`activity_line_items` 表（V34 migration）含下列欄位，**可直接作為單據層識別碼，不需新增欄位**：

| 欄位 | 用途 |
|---|---|
| `invoice_no` | 單據號（電費單、油單） |
| `invoice_date` | 單據日期 |
| `erp_ref` | ERP 參照號 |

⚠️ 這三個欄位在 `activity_line_items`，**不在 `activity_records`**。

### 3.2 比對鍵

v1.0 的 `factory_id + year + month + emission_source_id + invoice_no` 混用兩張表欄位，無法作為單一識別碼。改為**兩層**：

| 層級 | 比對鍵 | 語意 |
|---|---|---|
| **記錄層**（`activity_records`） | `factory_id + year + month + emission_source_id` | find-or-create，一組僅一列 |
| **單據層**（`activity_line_items`） | `activity_record_id + invoice_no` | 僅「更正既有單據」時使用（見 §3.4） |

**設計理由**：業務上的「同月多筆」實體是**單據**，不是記錄。記錄是這些單據的月加總容器（現行 `recomputeRecordFromLineItems()` 已是此語意：`activity_value = SUM(line_items.quantity)`）。

### 3.3 識別碼填寫率實測

現有 `activity_line_items` 共 **509 筆**：

| 欄位 | 有值筆數 | 填寫率 |
|---|---|---|
| `invoice_no` | 211 | **41%** |
| `erp_ref` | 283 | 56% |

**✅ 經確認，低填寫率為常態，不預期改善。** 各廠實務作業負擔下，要求全面補填不現實。

### 3.4 低填寫率的解法：由使用者宣告匯入模式（v1.2 全面改寫）

**v1.1 的方向是錯的。** v1.1 試圖「從資料反推哪筆是重複」，因此把識別碼當成必要的比對依據，並打算把「疑似重複」勾選介面做成一級功能。在 41% 填寫率下，這等於要求使用者每次面對數百筆待勾選項目，實際上會被全選放行，保護機制形同虛設。

**v1.2 改為在源頭消除問題。** 關鍵觀察：

> **識別碼只在「用 Excel 更正某一張既有單據」時才真正必要。** 其餘所有情況都不需要。

拆解三種實際作業情境：

| 情境 | 使用者意圖 | 系統需要識別碼嗎？ | 處理方式 |
|---|---|---|---|
| **① 整月完整檔** | 「這個源、這個月的完整明細就是這些，以此為準」 | **不需要** | 整批取代該 (源 × 月) 的所有明細。舊的全數移入 history，新的全數寫入。不可能重複 |
| **② 補單** | 「漏了幾張，補進去」 | **不需要** | 一律視為新增。若有 `quantity` + `invoice_date` 皆相同者，標黃提示「可能重複貼上」但**不阻擋**、不預設排除 |
| **③ 更正既有單據** | 「第 7 張單的數字打錯了要改」 | 需要 | **不支援匯入**。請於填報頁直接編輯該筆明細 |

**結果：41% 填寫率不再是問題。** 情境 ① ② 涵蓋絕大多數作業且完全不依賴識別碼；情境 ③ 改由填報頁手動處理，介面上直接看得到要改哪一筆，比用 Excel 對號更直觀也更不易出錯。

識別碼的角色從「**用來比對**」降級為「**佐證與追溯用**」——查證抽單時對得到即可，未填不影響系統正確性。

**現行程式碼的落差**：情境 ① 的行為（`DELETE` 舊明細再重插）**現行程式碼已經在做**，但缺三件事，這才是要補的：

1. ❌ 沒有告訴使用者「將刪除既有 N 筆」
2. ❌ 舊明細直接消失，沒有保留（無 history 表）
3. ❌ 已檢核／已封存的資料也照刪

### 3.5 識別碼欄位的但書（維持 2026-08-10 決議）

識別碼維持**非必填**，且**不改為部分必填**（v1.1 曾建議範疇一燃料與範疇二電力改必填，v1.2 依 §3.4 取消此建議——比對已不依賴識別碼，強制必填無實益，只增加填報負擔）。

但仍須揭露其對「追溯查證」的影響：

1. **範本內註明**：識別碼欄位標頭旁加註
   > 「建議填寫單據號／ERP 參照號，供查證追溯與 ERP 對帳使用。未填寫不影響系統計算。」

2. **填報介面提示**：欄位下方灰色提示文字（非錯誤、不阻擋送出）
   > 「單據號供查證抽查時追溯憑證使用，建議填寫。」

3. **匯入結果報告**：完成後的結果摘要單獨列出「未填識別碼筆數」，並可下載清單供填報單位自行核對。

**設計理由**：不強制必填以降低導入阻力；透過揭露使「查證抽單時找不到對應憑證」的責任明確落在填報單位。此揭露記錄對查證時說明資料品質管控亦有幫助。

---

## 四、匯入行為設計

### 4.1 兩條匯入路徑（現況）

`POST /api/records/import` 有兩條語意完全不同的路徑，模式必須分開定義：

| 路徑 | 適用分頁 | 現行行為 | 問題 |
|---|---|---|---|
| **A. 固定分頁**（月彙總） | `S2_電力`、`S1_燃料固定`、`S1_燃料移動`、`S1_逸散冷媒`、`S3_*` | 以 `廠×源×年月` 查既有記錄，`LIMIT 1` 取第一筆直接 UPDATE | 設計上就是「每月一筆彙總」，此路徑**做不到同月多筆**。無聲覆蓋，使用者不知道改了什麼 |
| **B. 單據明細** | `單據明細` / `S_單據明細` | `DELETE FROM activity_line_items WHERE activity_record_id = ?` 後全部重插 | 已是「整批取代」語意，但使用者不知道舊明細被刪除，且無保留 |

### 4.2 匯入模式（v1.2 改為使用者宣告）

**匯入前，使用者必須先選擇本次上傳的性質**（不提供「讓系統判斷」選項）：

#### 路徑 A — 固定分頁（月彙總）

一組 (廠 × 源 × 月) 本就只有一列，無「多筆」概念：

| 模式 | 行為 | 權限 |
|---|---|---|
| **僅新增**（預設） | 只寫尚無資料的月份，已有資料一律略過 | 全部 |
| **新增 + 更新** | 已有月份數值不同時列出 `舊值 → 新值`，逐筆或全選確認後覆蓋 | 全部 |

#### 路徑 B — 單據明細

| 模式 | 對應情境 | 行為 | 權限 |
|---|---|---|---|
| **整月完整檔**（預設） | §3.4 情境 ① | 整批取代該 (源 × 月) 的所有明細。舊明細移入 `activity_line_items_history`，可還原 | 全部 |
| **補單** | §3.4 情境 ② | 一律新增，不覆蓋既有任何一筆。`quantity` + `invoice_date` 皆相同者標黃提示「可能重複貼上」，**不阻擋、不預設排除** | 全部 |

> 情境 ③（更正既有單據）不提供匯入模式，於填報頁編輯。

### 4.3 匯入預覽（staging）必須顯示的內容

**「整月完整檔」模式**（破壞性最大，資訊要最完整）：

```
MK1 / 汽油（1-2A-1）/ 2026-03
  將以 12 筆 取代現有的 10 筆
  月總量：1,240 L → 1,388 L（+148 L，+11.9%）
  ⚠️ 本月已由永續發展部檢核完成，取代後檢核狀態將被取消
  舊明細將保留於變更記錄，可還原
```

**「補單」模式**：

```
MK1 / 汽油（1-2A-1）/ 2026-03
  新增 3 筆，現有 10 筆不變
  月總量：1,240 L → 1,388 L（+148 L）
  🟠 其中 1 筆（2026-03-14，48 L）與既有明細數值相同，可能是重複貼上
     ☑ 仍要匯入
```

### 4.4 逐列狀態標記

| 狀態 | 條件 | 預設動作 |
|---|---|---|
| 🟢 新增 | 該筆為新資料 | 寫入 |
| 🟡 更新 | 路徑 A「新增+更新」模式下數值不同 | **待使用者確認**（顯示 舊值 → 新值） |
| ⚪ 相同 | 數值完全相同 | 略過 |
| 🟠 可能重複 | 「補單」模式下 `quantity` + `invoice_date` 與既有明細相同 | **提示但仍匯入**（v1.2 改：不再預設排除） |
| 🔴 已封存 | 該 (廠, 年度) 已完成第三方查證封存 | **硬性拒絕**，不提供覆蓋選項 |

> **v1.2 對 🟠 的處理改變**：v1.1 為「預設不匯入，待勾選」。在 41% 填寫率下這會產生大量待勾選項目而被全選放行。改為「預設匯入 + 明顯提示」——因為在「補單」模式下使用者已明確宣告「這些是要新增的」，系統不該反過來否決其宣告；同日同量的兩張單在加油作業中本屬常見。

### 4.5 情境驗證

**情境 A — 年度未填完，重新上傳一份**
路徑 A「僅新增」：新月份寫入，已有月份略過，零風險。若要一併更正舊月份，改選「新增 + 更新」，只需處理黃色列。

**情境 B — 1~6 月已查核，現要匯入 7~12 月**
- 若 1~6 月僅為**部門檢核**（`is_reviewed = true`）：可覆蓋，跳警示確認（見 §5）
- 若 1~6 月已**第三方查證封存**：🔴 硬性拒絕，7~12 月正常新增

**情境 C — 上傳「整月完整檔」但實際上是補單檔（誤選模式）**
預覽頁明確顯示「將以 3 筆取代現有的 10 筆，月總量 1,240 L → 148 L（−88%）」。**大幅減量是誤選模式的主要訊號**，須以紅字強調總量降幅，並要求二次確認。舊明細保留於 history，誤選後可還原。

---

## 五、第一層保護：部門檢核（soft）

### 5.1 現況確認

`V1__initial_schema.sql` 第 130 行註解已載明「審查狀態（**取代 is_locked**）」，現行欄位為：

```
is_reviewed   BOOLEAN NOT NULL DEFAULT FALSE
reviewed_by   UUID REFERENCES users(id)
reviewed_at   TIMESTAMPTZ
```

**結論：不需改名，語意已正確**（原討論中的 `is_locked → is_checked` 改名作業取消）。

`reviewed_by` 目前 241 筆全為 NULL，處理方式見 §0.2。

### 5.2 行為定義

- 打勾代表「永續發展部已檢查過，數據無誤」，**不代表已通過第三方查證**
- 使用者要覆蓋已檢核的資料 → **跳警示，確認後即可覆蓋**
- 警示文字建議：
  > 「本月份已由永續發展部檢核完成。覆蓋後將取消檢核狀態，需重新檢核才會納入彙總與報告書。確定要覆蓋嗎？」

### 5.3 必須新增的連動邏輯

**人為修改**任一 `activity_records` 的活動數據時，自動執行：

```sql
is_reviewed = FALSE,
reviewed_by = NULL,
reviewed_at = NULL
```

**理由**：若數字被改但勾仍在，檢核者會誤以為已看過。

⚠️ **必須區分兩種「改動」，否則會與既有維運流程互撞：**

| 改動類型 | 是否清除 `is_reviewed` | 說明 |
|---|---|---|
| **人為改值**：填報頁編輯、Excel 匯入覆蓋／取代、API 傳入新的 `activity_value` | ✅ **清除** | 數字變了，檢核結論失效 |
| **系統重算**：改係數後的 `co2e = NULL → recalculate`（鐵則 3）、`recomputeScope2ForFactoryYear`（鐵則 4）、`recomputeRecordFromLineItems` 的純回算 | ❌ **不清除** | 活動數據未變，只是套用新係數／重新分攤。若一併清除，每次年度換係數就讓全平台數百筆檢核狀態歸零，逼使用者重勾一遍 |

**實作方式**：清除邏輯**不要**寫成資料庫 trigger（trigger 分不出呼叫來源）。改為在應用層的「活動數據寫入路徑」明確呼叫，計算類函式不呼叫。

### 5.4 UI 注意事項

依 Schema 註解「只有已審查的記錄才納入彙總與報告書」，覆蓋後該筆會**暫時從彙總數字消失**，直到重新打勾。此為正確行為，但須在介面明示，避免使用者誤以為資料遺失。

---

## 六、第二層保護：第三方查證封存（hard）

### 6.1 設計概念

封存 **不是**把檢核勾勾變硬，而是獨立的「年度封存（Freeze）」機制：以 **廠別 × 年度** 為單位，查證完成後將資料快照凍結。

### 6.2 新增資料表（`V41__verification_freeze.sql`）

> 檔名為 **V41**：DB 已套用至 V39，V40 為身分建置（§0.4）。

```sql
CREATE TABLE verification_periods (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    factory_id    UUID NOT NULL REFERENCES factories(id),
    year          INT  NOT NULL,
    status        VARCHAR(10) NOT NULL DEFAULT 'open'
                  CHECK (status IN ('open', 'verified')),
    verifier_org  VARCHAR(100),      -- 查證機構
    verified_date DATE,              -- 查證完成日
    frozen_by     UUID REFERENCES users(id),
    frozen_at     TIMESTAMPTZ,
    data_hash     CHAR(64),          -- SHA-256，防篡改
    UNIQUE (factory_id, year)
);
```

```sql
-- 唯讀快照表，欄位結構同 activity_records，另加：
CREATE TABLE activity_records_verified (
    ...（複製 activity_records 全部欄位）...
    version             INT  NOT NULL DEFAULT 1,
    restatement_reason  TEXT,
    snapshot_at         TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

**✅ 2026-08-11 決議：單據明細一併快照。**

```sql
-- 欄位結構同 activity_line_items，另加：
CREATE TABLE activity_line_items_verified (
    ...（複製 activity_line_items 全部欄位）...
    verified_record_id  UUID NOT NULL REFERENCES activity_records_verified(id),
    version             INT  NOT NULL DEFAULT 1,
    snapshot_at         TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

**理由**：現有 509 筆明細是 `activity_value` 的計算依據，查證單位抽查單據時會需要。若不一併快照，封存後主表明細被改，快照的 `activity_value` 就失去支撐憑證，防篡改驗證只證明了加總沒變、證明不了組成沒變。

### 6.3 封存流程

1. 具 `can_freeze` 權限者（Elodie / Johnson）於平台選擇「廠別 × 年度」執行封存
2. 系統將該範圍所有 `activity_records` 及其 `activity_line_items` 原封不動複製至快照表，**連同當時的 `emission_factor_id` 與計算結果**
3. 對整份快照（含明細）計算 SHA-256 雜湊值，寫入 `verification_periods.data_hash`
4. `status` 標記為 `verified`

⚠️ **雜湊計算須固定順序與序列化格式**（例如依 `id` 排序後串接指定欄位，明細依 `verified_record_id, id` 排序），否則日後重算會因列序不同而不符，防篡改驗證會誤報。此規則須寫入程式碼註解與維運手冊。

### 6.4 封存後的強制行為

- 快照表（`activity_records_verified`、`activity_line_items_verified`）於**資料庫層**只授予 SELECT
  - ⚠️ app 以單一 `DATABASE_URL` 連線，該角色很可能是表的 owner；對 owner `REVOKE UPDATE` 擋不住（owner 可自行 re-grant）。因此必須 **REVOKE 加上 `BEFORE UPDATE OR DELETE` trigger `RAISE EXCEPTION`**，雙保險，不依賴前端阻擋
- 主表該 (廠, 年度) 範圍的任何寫入、匯入、覆蓋 → **直接拒絕**，不提供「是否覆蓋」選項
  - 錯誤訊息：「本年度已完成第三方查證，資料已封存，無法修改。」
- **報告書匯出與對外揭露一律讀快照表**，不讀主表 → 主表日後即使被誤動，對外數字不變

### 6.5 防篡改驗證

日後若查證單位或稽核質疑數字遭修改，重算快照表之 SHA-256 並與 `data_hash` 比對即可自證。此設計對 ISO 14064 查證有加分作用。

### 6.6 與範疇二整年重算的衝突

專案鐵則 4：範疇二 iREC 走「年度基礎分攤」，電量或 REC 任一異動就必須呼叫 `recomputeScope2ForFactoryYear(factory_id, year)` **整年重算**。

**衝突**：封存後若有人為同年度補登或修改 iREC 憑證，這支重算函式會直接 UPDATE 已封存年度的 `activity_records`，**繞過 §6.4 的「主表拒絕寫入」**（那是 API 層檢查，重算是內部函式呼叫）。

**對策（必做）**：

1. `recomputeScope2ForFactoryYear` 開頭查 `verification_periods`，該 (廠, 年度) 為 `verified` 時**直接 return 並記錄警告**，不做任何 UPDATE
2. `rec_certificates` 的新增／修改 API 同樣加封存年度檢查，源頭阻擋
3. 同理檢查其他會整批 UPDATE 主表的路徑：`recalculate`（鐵則 3 的 co2e 補算）、`recomputeRecordFromLineItems`

**若漏了這一步，封存機制會有一個安靜的破口**——對外數字讀快照不受影響，但主表與快照不一致，查證單位比對時會發現。

---

## 七、查證後的更正（Restatement）

查證後仍可能發現遺漏（例如漏計一台鍋爐）。**不開放修改既有封存資料**，改以版本化處理：

- 原 `version = 1` 快照永久保留不動
- 更正後產生 `version = 2`，並填寫 `restatement_reason`
- 報告書預設讀取最新 version，但保留查閱歷史版本的能力

此設計對應 GHG Protocol「基準年重新計算（Base Year Recalculation）」要求。**2025 為聚陽基準年，未來數年必然會用到。**

> 註：2025 年度目前有 78 筆填報記錄（70 筆已檢核），為基準年封存標的。

---

## 八、封存權限管理

### 8.1 設計原則

**不得**將封存權限硬編碼綁定特定帳號（email 或 UUID）。人員異動時將導致無人可封存，且需修改程式碼。

### 8.2 權限欄位

於 `V40__user_identity.sql` 一併加入（見 §0.4）：

```sql
ALTER TABLE users ADD COLUMN can_freeze BOOLEAN NOT NULL DEFAULT FALSE;
```

### 8.3 授權方式（三層後路）

| 層級 | 方式 |
|---|---|
| 1. 初始 | Elodie Cheng、Johnson Lin `can_freeze = true`（見 §0.5） |
| 2. 日常 | Admin 於後台「帳號管理」頁面勾選授予他人，**不需改程式碼**（本輪不做，見 §0.6） |
| 3. 緊急 | 執行 `node scripts/grant-freeze.mjs <email>`，具 DB 連線者可直接授權（接手人的後路） |

所有授權 / 取消授權動作須寫入 `audit_log`。

⬜ `audit_log` 表目前不存在，需一併建立（併入 §9 的稽核留痕 migration）。

### 8.4 單點失效（✅ 已解決）

~~目前僅 Elodie 一人具備 `can_freeze`~~
**2026-08-11 決議**：新增主管 Johnson Lin（JohnsonLin@makalot.com.tw）為第二位 `can_freeze` 持有者。Kelly 與 Meng-Ying 不授予。

---

## 九、稽核留痕

新增 `import_batches` 表，記錄每次匯入：

- 批次 ID、匯入者、匯入時間、檔名
- **匯入路徑**（固定分頁 / 單據明細）+ **宣告模式**（僅新增 / 新增+更新 / 整月完整檔 / 補單）
- 各狀態筆數統計（新增 N、更新 N、取代 N、略過 N、拒絕 N、未填識別碼 N）

新增 `activity_records_history` 與 `activity_line_items_history`，記錄每次覆蓋／取代的**舊值**、變更者、所屬批次，供查證回溯與誤操作還原（§4.5 情境 C 依賴此表）。

新增 `audit_log` 表（§8.3 需要），記錄權限授予/取消、封存/解封等敏感操作。

---

## 十、開發任務清單

封存的權限與留痕皆掛在使用者身分上，故身分建置排最前。

**⏰ Deadline（2026-08-11 確認）：Phase 2 建置 08-17 完成、全案 08-30 完成。**

| # | 任務 | 狀態 | 優先 | 前置 |
|---|---|---|---|---|
| 0a | `V40__user_identity.sql`：`password_hash` / `role` / `factory_id` / `can_freeze` | ✅ **已套用**（08-11） | 高 | — |
| 0b | `lib/auth.ts` 改查 `users` 表 + bcrypt；移除硬寫帳密；新增 `lib/session.ts`（`requireUser` / `requireAdmin` / `requireFreeze` / `requireFactoryAccess`） | ✅ **已完成**（08-11，build 通過、登入成功與拒絕路徑皆已驗證） | 高 | 0a |
| 0c | `scripts/create-user.mjs`（`--init` / `--list` / `--reset`） | ✅ 腳本完成；⬜ **待 Elodie 執行 `--init` 建立四個帳號（需人工輸入密碼）** | 高 | 0a |
| 0d | 登入頁「帳號」改為 email | ✅ **已完成**（08-11） | 高 | 0b |
| 0e | `scripts/backfill-reviewed-by.mjs` 回填 176 筆 `reviewed_by`（§0.2） | ✅ 腳本完成；⬜ 待 0c 完成後執行 | 中 | 0c |
| 0f | **部署**：`npm run build && pm2 restart ghg-platform && git push` | ⬜ **必須在 0c 之後**（見下方警告） | 高 | 0c |
| 1 | 人為改值時清除 `is_reviewed` 連動邏輯（**須區分系統重算**，§5.3） | ⬜ | 高 | — |
| 2 | `V41__verification_freeze.sql`：`verification_periods` + 兩張快照表（含明細） | ✅ **已套用**（08-11，18 項約束與唯讀行為已驗證） | 高 | 0a |
| 3 | 快照表唯讀：REVOKE + trigger 雙保險（§6.4） | ✅ **已完成**（含於 V41，UPDATE / DELETE 實測被拒） | 高 | 2 |
| 4 | **封存年度寫入阻擋**：API 層 + `recomputeScope2ForFactoryYear` / `recalculate` 內部檢查（§6.6） | ⬜ | **高（漏了會有安靜破口）** | 2 |
| 5 | history 表（`activity_records_history` / `activity_line_items_history`）+ 還原功能 | ⬜ | **高**（§4.2 整月取代模式的安全網） | — |
| 6 | 匯入模式宣告 UI + staging 預覽（§4.2、§4.3） | ⬜ | 高 | 5 |
| 7 | 封存 / 解封 UI 與 SHA-256 計算（含固定序列化規則） | ⬜ | 高 | 0b, 2 |
| 8 | `scripts/grant-freeze.mjs`（含「不可取消最後一位持有者」保護） | ✅ **已完成**（08-11） | 中 | 0a |
| 9 | `import_batches` + `audit_log` | ⬜ | 中 | 0b |
| 10 | 識別碼但書文案（範本註記／介面提示／結果清單下載） | ⬜ | 中 | 6 |
| 11 | 報告書匯出改讀快照表 | ⬜ | 中 | 2 |
| 12 | `/admin/users` 帳號管理頁（§8.3 第 2 層） | ⬜ | 中 | 0b |
| 13 | Azure AD SSO 接線（替換 `authorize()`，補 `azure_oid`） | ⬜ | 中（等 IT，不擋上線） | 0b |
| 14 | 現有 API 補 reporter 廠別過濾（獨立一輪，逐頁驗證） | ⬜ | 低（現階段全為 admin） | 0b |
| 15 | Restatement 版本化機制 | ⬜ | 低（查證後才需要） | 2 |

### ⚠️ 部署順序警告

`lib/auth.ts` 的硬寫帳密（admin / ghg2025）**已移除**。若在建立帳號前就部署，
登入頁將沒有任何可用帳號。**正確順序：**

```
1. node scripts/create-user.mjs --init     # 建立四個帳號（人工輸入密碼）
2. node scripts/backfill-reviewed-by.mjs   # 回填 176 筆檢核者
3. cd apps/web && npm run build && pm2 restart ghg-platform && git push
```

> 註：現行 `src/middleware.ts` 實際上並未阻擋任何路由（生產環境的白名單判斷後
> 仍一律 `NextResponse.next()`），因此部署後頁面本身不會被鎖住，但登入流程會無帳號可用。
> middleware 是否要真正啟用強制登入，屬另一項待確認事項。

---

## 十一、待確認事項

**已決議：**
- ✅ 匯入範本識別碼欄位 → 維持非必填（2026-08-10 決議，v1.2 確認不改為部分必填，§3.5）
- ✅ 第二位 `can_freeze` 人選 → Johnson Lin；Kelly / Meng-Ying 不授予（2026-08-11，§0.5、§8.4）
- ✅ 登入方式 → 先做密碼登入過渡，SSO 之後接（2026-08-11，§0.3）
- ✅ 比對鍵層級 → 下移至 line item 層（2026-08-11，§3.2）
- ✅ 識別碼低填寫率解法 → 由使用者宣告匯入模式，不靠系統反推（2026-08-11，§3.4）
- ✅ 匯入習慣 → 整月完整檔與補單兩種都有，故兩種模式都做（2026-08-11，§4.2）
- ✅ 176 筆檢核者 → 確認僅 Elodie 一人操作，可 backfill（2026-08-11，§0.2）
- ✅ 單據明細一併封存快照（2026-08-11，§6.2）

**未決：**
- ⬜ **第一家廠的第三方查證完成日**——Project 內有 `2026 ISO140641 查證計畫.xlsx`，尚未開啟確認。
  （已知專案 deadline：Phase 2 於 2026-08-17、全案 2026-08-30。查證日仍須確認以驗證此時程足夠）
- ⬜ `middleware.ts` 是否要真正啟用強制登入？現行版本在生產環境也是一律放行
- ⬜ 密碼保管方式（循公司密碼保管流程，具體流程待確認）
- ⬜ `reviewed_at` 無從回溯，查證時的說明方式

---

## ⏰ 時程提醒

**封存功能必須在第一家廠完成第三方查證前上線**，否則查證完成的資料仍處於可修改狀態，查證單位可能開立不符合事項。

v1.2 改走密碼登入過渡後，**關鍵路徑上已無外部依賴（不再等 IT 排程）**，deadline 風險大幅降低。Azure AD 接線降為後續替換工作（任務 13），不阻擋封存上線。

**仍須儘速確認 `2026 ISO140641 查證計畫.xlsx` 中最早的查證完成日，回推開發 deadline。**

---

*本文件為設計草案，供內部開發參考。所有 AI 產出僅供參考，需經人工複核；涉及查證與對外揭露的結論需永續發展部及查證單位確認。實作前請確認第十一節待確認事項。*
